<h3 class="mb-4">
  {{title}}
</h3>

<div class="row">
  <div class="col-md-12">
    <!-- Formulario de Proveedor -->
    <div class="card card-primary">
      <div class="card-header">
        <h4 class="card-title">Llena los datos de la marca</h4>
      </div>
      <!-- /.card-header -->
      <!-- form start -->
      <form action="{{route}}" method="POST">
        <div class="card-body">
          <div class="form-group">
            <label for="marca">Marca</label>
            <input type="text" class="form-control" id="marca" name="marca" value="{{marca.marca}}" autofocus required>
          </div>
        </div>
        <!-- /.card-body -->

        {{#if marca}}
          <input type="hidden" value="{{marca.id_marca}}" name="id_marca">
        {{/if}}
        <div class="card-footer">
          <button type="submit" class="btn btn-primary">Guardar</button>
        </div>
      </form>
      <!-- Form end -->

    </div>
    <!-- /.card -->
  </div>
</div>